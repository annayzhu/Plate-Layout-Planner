{
  "name": "plate-layout-planner",
  "version": "0.1.0",
  "private": true,
  "description": "Dependency-free, browser-local microplate layout editor",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "serve": "python3 -m http.server 4186",
    "test:visual": "node scripts/visual-smoke.mjs artifacts/visual-smoke"
  }
}
